# 核心依赖
opencv-python>=4.8.0
pyautogui>=0.9.54
numpy>=1.23.0
psutil>=5.9.0

# 调度与自动化
APScheduler>=3.10.0
playwright>=1.40.0

# 兼容旧版 Python 的 TOML 解析
tomli>=2.0.1; python_version < "3.11"

# Windows 平台增强能力
pywin32>=306; sys_platform == "win32"
