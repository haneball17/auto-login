# 常用命令
- 安装依赖：`poetry install`
- 运行主程序：`poetry run python -m src.main`
- 仅执行启动器阶段：`poetry run python -m src.main --launcher-only`
- 运行前端：`poetry run python -m src.ui`（前端完成后）
- 运行测试：`poetry run pytest`
